contacts = {}

# Подсказка для add_contant:
def add_contact(name, phone):
    contants[________] = ________

# Подсказка для find_contact:
def find_contact(name):
    if name in contacts:  # проверяем, есть ли ключ в словаре
        return ________   # верни телефон
    else:
        return "Контакт не найден"