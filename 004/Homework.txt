1) Сравнение list и set
    Создать стороковую переменную string = 'AAAABBBBCCCC'
    Создать переменную l = list(string)
    Создать переменную s = set(string)
    Вывести переменные l и s в консоль с помощью функции print
    Объяснить наблюдаемый вывод

2) Что хранят встроенные типы данных
    Создать список. Добавить в него 4 элемента разных типов.
    Создать множество. Добавить в него 4 элемента разных типов.
    Создать словарь. Добавить в него 4 пары ключ-значение. Значения должны быть разных типов, ключи могут быть строками, числами или кортежами.
    Создать кортеж. Добавить в него 4 элемента разных типов.

3) Позиционные аргументы
    Создать функцию add сложения трёх чисел.
    Вызвать функцию add с позиционным способом передачи аргументов (смотри 003/lesson.py).

4) Именованные аргументы
    Создать функцию sub с двумя параметрами a и b, которая вычитает второй параметр из первого.
    Вызвать функцию sub с именованным способом передачи аргументов (смотри 003/lesson.py).

5) Проверка ключей в словаре
    Создать функцию, которая проверяет, что заданный ключ присутствует в словаре. Функция должна принимать два параметра, 1 - словарь, в котором искать, 2 - ключ, который нужно проверить. Функция должна возвращать True или False, в зависимости от того, присутствует ли искомый ключ в переданном словаре. (смотри 004/blitz.py)

6) Объяснить что делает эта программа:
string = 'Hello from Saint-Petersburg'
index = 0
while index < len(string):
    if index % 2 == 0:
        print(string[index])
    index += 1