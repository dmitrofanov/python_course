"""
Представь, что функции — это кухонные приборы.
func1 — это блендер (измельчает), func2 — это духовка (запекает).
func2(func1(ингредиенты)) — значит: 1) измельчи ингредиенты в блендере, 2) запеки полученное в духовке.
"""

"""
ЗАДАЧА 1: Две очевидные функции

1. Напиши функцию double(x), которая возвращает число, умноженное на 2
2. Напиши функцию add_five(x), которая возвращает число + 5

Протестируй цепочку:
1.0) Сначала примени add_five к числу 10, результат сохрани в переменную
1.1) Потом примени double к этому результату
2.0) Теперь сделай то же самое в одну строку: double(add_five(10))

Оба способа должны дать одинаковый результат!
"""
