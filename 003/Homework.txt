1) Создать функцию, которая принимает список слов и печатает каждое слово с новой строки.
2) Создать функцию, которая принимает строку и возвращает список слов в этой строке.
3) Создать функцию, которая принимает строку и печатает каждое слово с новой строки. Использовать функции, созданные в заданиях 1 и 2.
4) Создать функцию, печатающую таблицу умножения для заданного числа
5) Создать функцию, принимающую список оценок от 1 до 5 и возвращающую среднюю оценку
6) Создать функцию, принимающую список оценок от 1 до 5, рассчитывающую среднюю оценку и печатающую среднюю оценку текстом ("кол", "два", "три", "четыре", "пять"). Использовать функцию созданную в задании 5.
7) Создать функцию, принимающую имя, город и возраст и возвращающую словарь, в котором представлены эти данные.
8) Создать функцию, принимающую список чего либо и возвращающую множество, созданное из данных принятого списка.

Задания со звёздочкой (усложнённые):
* Создать функцию, которая проверяет, является ли число простым числом. Функция должна принимать число, а потом проверять, что это число не делится нацело (number % i == 0) ни на одно число, кроме единицы и самого себя. Использовать цикл for по всем числам от двойки до проверяемого числа (пригодится функция range). Для примера можно загуглить "python is_prime function manually".